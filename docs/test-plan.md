# Test Plan

| Test Case ID | Test Type | Case Type | Description | Pre-condition | Test Steps | Test Data | Expected Result | Priority |
|---|---|---|---|---|---|---|---|---|
| UT-001 | Unit Test | Positive | สร้าง ticket ด้วย `category` และ `priority` ที่ถูกต้องและให้ระบบกำหนด `id` พร้อม `status` เริ่มต้น | Seed เปิดหรือปิดก็ได้ และ in-memory store พร้อมใช้งาน | 1) เรียก service create ticket 2) ตรวจค่าที่คืนมา | `category=Hardware`, `priority=High` | ได้ ticket ใหม่ที่มี `id` รูปแบบ `TKT-xxx` และ `status=Open` | High |
| UT-002 | Unit Test | Negative | ไม่อนุญาตให้ create ticket เมื่อขาด required field | service พร้อมใช้งาน | 1) เรียก service create ticket โดยไม่ส่ง `priority` | `category=Software` | ระบบปฏิเสธคำขอด้วย validation error | High |
| UT-003 | Unit Test | Negative | ไม่อนุญาตค่า `category` ที่อยู่นอกชุดที่กำหนด | service พร้อมใช้งาน | 1) เรียก service create ticket 2) ส่งค่า category ที่ไม่ถูกต้อง | `category=Printer`, `priority=Low` | ระบบปฏิเสธคำขอด้วย validation error | High |
| UT-004 | Unit Test | Negative | ไม่อนุญาตค่า `priority` ที่อยู่นอกชุดที่กำหนด | service พร้อมใช้งาน | 1) เรียก service create ticket 2) ส่งค่า priority ที่ไม่ถูกต้อง | `category=Network`, `priority=Urgent` | ระบบปฏิเสธคำขอด้วย validation error | High |
| UT-005 | Unit Test | Positive | ตรวจว่า service คืนรายการทั้งหมดจาก in-memory list | มี ticket อย่างน้อย 3 รายการ | 1) เรียก service list all 2) ตรวจจำนวนรายการ | Seed 3 รายการ | คืนรายการครบทุก ticket ที่มีอยู่ | Medium |
| UT-006 | Unit Test | Positive | กรอง ticket ตาม `status` ได้ถูกต้อง | มี ticket ครบทั้ง `Open`, `In Progress`, `Resolved` | 1) เรียก service filter by status 2) ตรวจผลลัพธ์ | `status=Open` | คืนเฉพาะ ticket ที่สถานะ `Open` | High |
| UT-007 | Unit Test | Edge Case | กรองแล้วไม่พบ ticket ต้องได้ empty list | มี ticket แต่ไม่มีสถานะที่เลือก | 1) เรียก service filter by status 2) ตรวจผลลัพธ์ | `status=Open` ในกรณีไม่มี ticket Open | คืนรายการว่าง ไม่เกิด error | Medium |
| UT-008 | Unit Test | Positive | ดึง ticket ตาม `id` ที่มีอยู่ได้ | มี ticket ที่ id ตรงกับข้อมูลใน store | 1) เรียก service get by id | `id=TKT-001` | คืน ticket ที่ตรงกับ id | High |
| UT-009 | Unit Test | Negative | ดึง ticketด้วย `id` ที่ไม่มีอยู่ต้องไม่พบข้อมูล | service มีรายการอยู่แต่ไม่มี id ที่ระบุ | 1) เรียก service get by id | `id=TKT-999` | ระบบแจ้งว่าไม่พบ ticket | High |
| UT-010 | Unit Test | Edge Case | ตรวจรูปแบบ `id` ให้ตรง `TKT-xxx` | service พร้อมใช้งาน | 1) เรียก validation/lookup ด้วย id ที่ไม่ตรงรูปแบบ | `id=TK-001` | ระบบปฏิเสธค่า id ที่ไม่ตรงรูปแบบ | High |
| UT-011 | Unit Test | Positive | อัปเดตสถานะ ticket ได้ด้วยค่าที่อนุญาต | มี ticket ที่ยังไม่ถูกอัปเดต | 1) เรียก service update status 2) ตรวจค่าหลังอัปเดต | `id=TKT-001`, `status=Resolved` | ticket ถูกอัปเดตเป็น `Resolved` | High |
| UT-012 | Unit Test | Negative | ไม่อนุญาตค่า status ที่ไม่อยู่ในชุดที่กำหนด | มี ticket อยู่ใน store | 1) เรียก service update status 2) ส่งค่าที่ไม่ถูกต้อง | `id=TKT-001`, `status=Closed` | ระบบปฏิเสธคำขอด้วย validation error | High |
| UT-013 | Unit Test | Edge Case | การอัปเดตเป็น status เดิมต้องไม่ทำให้ข้อมูลเพี้ยน | มี ticket ที่ status ตรงกับค่าที่จะส่ง | 1) เรียก service update status ด้วยค่าเดิม 2) ตรวจรายการและ summary | `id=TKT-001`, `status=Open` | ticket ยังถูกนับถูกต้องและ summary counts ไม่ผิดเพี้ยน | Medium |
| UT-014 | Unit Test | Positive | summary counts ต้องตรงกับจำนวน ticket ใน in-memory list | มี ticket ครบ 3 สถานะ | 1) เรียก service summary 2) ตรวจค่าที่คืน | Seed 3 รายการ | คืน total และ count รายสถานะตรงกับข้อมูลจริง | High |
| UT-015 | Unit Test | Edge Case | summary counts ต้องคำนวณจากข้อมูลปัจจุบันทุกครั้ง | มีการเพิ่มหรืออัปเดต ticket แล้ว | 1) เปลี่ยนข้อมูลใน list 2) เรียก summary ซ้ำ | เพิ่ม ticket หรือเปลี่ยน status | summary สะท้อนข้อมูลล่าสุด ไม่ใช้ค่า cache | Medium |
| UT-016 | Unit Test | Positive | seed เปิดต้องโหลดข้อมูลเริ่มต้นอย่างน้อย 3 รายการ | seed control เปิด | 1) initialize service/app 2) ตรวจ state เริ่มต้น | seed enabled | มีข้อมูลอย่างน้อย 3 รายการและครบ `Open`, `In Progress`, `Resolved` | High |
| UT-017 | Unit Test | Positive | seed ปิดต้องเริ่มจาก state ว่าง | seed control ปิด | 1) initialize service/app 2) ตรวจ state เริ่มต้น | seed disabled | รายการ ticket เริ่มต้นว่าง | High |
| IT-001 | Integration Test | Positive | สร้าง ticket ผ่าน `POST /tickets` สำเร็จ | API พร้อมใช้งาน | 1) ส่ง POST 2) ตรวจ response 3) ตรวจว่า ticket ถูกเพิ่มใน list | `category=Hardware`, `priority=Medium` | ได้ `201 Created` พร้อม ticket ใหม่, `id`, และ `status=Open` | High |
| IT-002 | Integration Test | Negative | `POST /tickets` ต้องปฏิเสธเมื่อขาด required field | API พร้อมใช้งาน | 1) ส่ง POST ที่ขาด field 2) ตรวจ response | ขาด `priority` | ได้ `422 Unprocessable Entity` | High |
| IT-003 | Integration Test | Negative | `POST /tickets` ต้องปฏิเสธค่า category ที่ไม่ถูกต้อง | API พร้อมใช้งาน | 1) ส่ง POST 2) ตรวจ response | `category=Printer`, `priority=Low` | ได้ `422 Unprocessable Entity` | High |
| IT-004 | Integration Test | Negative | `POST /tickets` ต้องปฏิเสธค่า priority ที่ไม่ถูกต้อง | API พร้อมใช้งาน | 1) ส่ง POST 2) ตรวจ response | `category=Account`, `priority=Urgent` | ได้ `422 Unprocessable Entity` | High |
| IT-005 | Integration Test | Positive | `GET /tickets` ต้องคืนรายการทั้งหมด | มี ticket ในระบบ | 1) ส่ง GET /tickets 2) ตรวจ response body | ไม่มี query | ได้รายการ ticket ทั้งหมดในระบบ | High |
| IT-006 | Integration Test | Positive | `GET /tickets?status=...` ต้อง filter ตามสถานะ | มี ticket ครบ 3 สถานะ | 1) ส่ง GET พร้อม query status 2) ตรวจ response | `status=In Progress` | คืนเฉพาะ ticket สถานะ `In Progress` | High |
| IT-007 | Integration Test | Edge Case | `GET /tickets?status=...` เมื่อไม่มีข้อมูลต้องคืน empty array | ไม่มี ticket ในสถานะที่เลือก | 1) ส่ง GET พร้อม query status 2) ตรวจ response | `status=Resolved` เมื่อไม่มี ticket Resolved | ได้ array ว่าง ไม่ใช่ error | Medium |
| IT-008 | Integration Test | Positive | `GET /tickets/{id}` ต้องคืน ticket ที่พบ | มี ticket id ตรงกับที่ร้องขอ | 1) ส่ง GET /tickets/{id} 2) ตรวจ response | `id=TKT-001` | ได้ `200 OK` และ ticket ที่ตรงกับ id | High |
| IT-009 | Integration Test | Negative | `GET /tickets/{id}` ต้องคืน 404 เมื่อไม่พบ ticket | API พร้อมใช้งาน | 1) ส่ง GET /tickets/{id} 2) ตรวจ response | `id=TKT-999` | ได้ `404 Not Found` | High |
| IT-010 | Integration Test | Edge Case | `GET /tickets/{id}` ต้องปฏิเสธ id ที่ไม่ตรงรูปแบบ | API พร้อมใช้งาน | 1) ส่ง GET /tickets/{id} 2) ตรวจ response | `id=TK-001` | ได้ validation error ตามรูปแบบ id | High |
| IT-011 | Integration Test | Positive | `PATCH /tickets/{id}/status` ต้องอัปเดต status สำเร็จ | มี ticket ที่พร้อมอัปเดต | 1) ส่ง PATCH 2) ตรวจ response 3) เรียก list/summary ซ้ำ | `id=TKT-001`, `status=Resolved` | ได้ `200 OK` และสถานะเปลี่ยนเป็น `Resolved` | High |
| IT-012 | Integration Test | Negative | `PATCH /tickets/{id}/status` ต้องปฏิเสธค่า status ที่ไม่ถูกต้อง | มี ticket ในระบบ | 1) ส่ง PATCH 2) ตรวจ response | `id=TKT-001`, `status=Closed` | ได้ `422 Unprocessable Entity` | High |
| IT-013 | Integration Test | Negative | `PATCH /tickets/{id}/status` ต้องคืน 404 เมื่อไม่พบ ticket | API พร้อมใช้งาน | 1) ส่ง PATCH 2) ตรวจ response | `id=TKT-999`, `status=Open` | ได้ `404 Not Found` | High |
| IT-014 | Integration Test | Positive | `GET /summary` ต้องคืน counts ตรงกับข้อมูลจริง | มี ticket ครบ 3 สถานะ | 1) ส่ง GET /summary 2) ตรวจ response | Seed 3 รายการ | คืน `total` และ count รายสถานะตรงกับ list จริง | High |
| IT-015 | Integration Test | Edge Case | summary ต้องเปลี่ยนตามข้อมูลล่าสุดหลัง create หรือ update | มีการเพิ่ม ticket หรืออัปเดต status แล้ว | 1) ทำ create หรือ update 2) เรียก GET /summary 3) เทียบกับ GET /tickets | ticket ใหม่หรือ status ใหม่ | summary สอดคล้องกับข้อมูลล่าสุดเสมอ | High |
| IT-016 | Integration Test | Edge Case | seed เปิดต้องมี initial state ที่ทดสอบได้ครบ 3 สถานะ | เปิด seed ก่อนเรียก API | 1) เริ่มระบบ 2) เรียก GET /tickets และ GET /summary | seed enabled | มีอย่างน้อย 3 ticket และ summary ครบ `Open`, `In Progress`, `Resolved` | Medium |
| IT-017 | Integration Test | Edge Case | seed ปิดต้องทำให้ระบบเริ่มจาก state ว่างเพื่อให้ test คุมข้อมูลเองได้ | ปิด seed ก่อนเริ่มระบบ | 1) เริ่มระบบ 2) เรียก GET /tickets และ GET /summary | seed disabled | ไม่มี ticket เริ่มต้น และ summary เป็นศูนย์ทั้งหมด | Medium |